\section{Module \texttt{demo\_app.c}}

Le module \texttt{demo\_app.c} fournit le point d'entrée public de l'application de démonstration. Il installe le thème, crée la fenêtre principale via le wrapper, puis injecte le contenu de la démo.

\subsection{Fonction \texttt{demo\_app\_start}}

\begin{lstlisting}[language=C]
void demo_app_start(GtkApplication *app, gpointer user_data) {
  (void)user_data;
  static bool once = false;
  if (!once) {
    apply_demo_theme();
    once = true;
  }

  GtkWidget *window = create_window(app, &(window_config){
      .title = "GTK UI Wrapper Showcasea",
      .icon_name = "applications-development-symbolic",
      .widget_name = "wrapper-window",
      .background_image_path = NULL,
      .default_width = 700,
      .default_height = 300,
      .min_width = 400,
      .min_height = 300,
      .present_on_create = false,
  });

  showcase_state *state = g_new0(showcase_state, 1);

  gtk_window_set_child(GTK_WINDOW(window),
                       build_showcase(app, GTK_WINDOW(window), state));
  gtk_window_present(GTK_WINDOW(window));
}
\end{lstlisting}

\newpage
